import {requirePageUser} from "@/lib/auth";import Link from "next/link";import EquipmentManager from "@/components/EquipmentManager";export default async function EquipmentPage(){await requirePageUser(["admin"]);return <main className="shell"><div className="topbar"><Link className="back" href="/">← Zur Übersicht</Link></div><section className="hero"><span className="pill">Geräteverwaltung</span><h1>Trocknungsgeräte</h1><p className="muted">Geräte einmal anlegen und einer Kategorie zuordnen. Bei der Trocknung erscheinen anschließend nur die passenden Geräte.</p></section><EquipmentManager/></main>}
