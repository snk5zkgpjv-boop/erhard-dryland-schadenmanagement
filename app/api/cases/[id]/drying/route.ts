import {getSql} from "@/lib/db";function txt(v:unknown){const t=typeof v==="string"?v.trim():"";return t===""?null:t}export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,sql=getSql();const rows=await sql`SELECT d.id,d.room,d.installed_at,d.removed_at,d.drying_method,d.notes,c.name AS company_name,e.id equipment_id,e.equipment_code,e.name equipment_name,e.equipment_type FROM drying_installations d JOIN companies c ON c.id=d.company_id LEFT JOIN equipment e ON e.id=d.equipment_id WHERE d.case_id=${id} ORDER BY d.installed_at DESC,e.name`;return Response.json(rows)}catch(e){return Response.json({error:e instanceof Error?e.message:"Trocknung konnte nicht geladen werden."},{status:500})}}export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,b=await r.json(),sql=getSql(),idsIn=Array.isArray(b.equipment_ids)?b.equipment_ids.filter(Boolean):[];const ids:string[]=[];if(idsIn.length===0){const rows=await sql`INSERT INTO drying_installations(case_id,company_id,room,installed_at,removed_at,drying_method,notes) VALUES(${id},${txt(b.company_id)},${txt(b.room)},COALESCE(${txt(b.installed_at)}::timestamptz,now()),${txt(b.removed_at)}::timestamptz,${txt(b.drying_method)},${txt(b.notes)}) RETURNING id`;ids.push(rows[0].id as string)}else{for(const eq of idsIn){const rows=await sql`INSERT INTO drying_installations(case_id,company_id,equipment_id,room,installed_at,removed_at,drying_method,notes) VALUES(${id},${txt(b.company_id)},${String(eq)},${txt(b.room)},COALESCE(${txt(b.installed_at)}::timestamptz,now()),${txt(b.removed_at)}::timestamptz,${txt(b.drying_method)},${txt(b.notes)}) RETURNING id`;ids.push(rows[0].id as string)}}return Response.json({ids},{status:201})}catch(e){return Response.json({error:e instanceof Error?e.message:"Trocknung konnte nicht gespeichert werden."},{status:500})}}
