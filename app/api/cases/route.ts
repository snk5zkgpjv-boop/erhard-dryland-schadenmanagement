import {getSql} from "@/lib/db";
function clean(v:unknown){const t=typeof v==="string"?v.trim():"";return t===""?null:t}
export async function GET(){try{const sql=getSql();const rows=await sql`SELECT c.id,c.title,c.case_number,c.status,c.damage_type,c.object_street,c.object_postal_code,c.object_city,c.created_at,co.code AS company_code,co.name AS company_name FROM cases c JOIN companies co ON co.id=c.company_id ORDER BY c.created_at DESC`;return Response.json(rows)}catch(error){return Response.json({error:error instanceof Error?error.message:"Schadensfälle konnten nicht geladen werden."},{status:500})}}
export async function POST(request:Request){try{const body=await request.json();const companyId=clean(body.company_id);const title=clean(body.title);if(!companyId||!title)return Response.json({error:"Firma und Bezeichnung sind Pflichtfelder."},{status:400});const sql=getSql();const firstName=clean(body.customer_first_name),lastName=clean(body.customer_last_name),companyName=clean(body.customer_company_name);let customerId:string|null=null;if(firstName||lastName||companyName){const customers=await sql`INSERT INTO customers(kind,company_name,first_name,last_name) VALUES (${companyName?"company":"person"},${companyName},${firstName},${lastName}) RETURNING id`;customerId=customers[0].id as string}const rows=await sql`INSERT INTO cases(company_id,customer_id,case_number,title,status,damage_type,object_street,object_postal_code,object_city,floor,unit,insurer,insurance_number,claim_number,reference_number,damage_cause,damage_description,recommended_action,started_at) VALUES (${companyId},${customerId},${clean(body.case_number)},${title},'neu',${clean(body.damage_type)||'wasserschaden'},${clean(body.object_street)},${clean(body.object_postal_code)},${clean(body.object_city)},${clean(body.floor)},${clean(body.unit)},${clean(body.insurer)},${clean(body.insurance_number)},${clean(body.claim_number)},${clean(body.reference_number)},${clean(body.damage_cause)},${clean(body.damage_description)},${clean(body.recommended_action)},now()) RETURNING id`;return Response.json({id:rows[0].id},{status:201})}catch(error){return Response.json({error:error instanceof Error?error.message:"Schaden konnte nicht angelegt werden."},{status:500})}}
