import {headers} from "next/headers";import {ensureAuthSchema,hashPassword} from "@/lib/auth";import {getSql} from "@/lib/db";
async function admin(){const h=await headers();return h.get("x-app-role")==="admin"}
export async function GET(){if(!await admin())return Response.json({error:"Nur Admin."},{status:403});await ensureAuthSchema();const sql=getSql();const rows=await sql`SELECT id,username,display_name,email,role,active,created_at FROM app_users ORDER BY display_name`;return Response.json(rows)}
export async function POST(r:Request){if(!await admin())return Response.json({error:"Nur Admin."},{status:403});try{const b=await r.json();const username=String(b.username||"").trim(),display=String(b.display_name||"").trim(),password=String(b.password||""),role=String(b.role||"techniker");if(!username||!display||password.length<8||!["admin","techniker","buero"].includes(role))return Response.json({error:"Bitte alle Pflichtfelder korrekt ausfüllen."},{status:400});const sql=getSql();const rows=await sql`INSERT INTO app_users(username,display_name,email,role,password_hash) VALUES(${username},${display},${String(b.email||"").trim()||null},${role},${hashPassword(password)}) RETURNING id`;return Response.json({id:rows[0].id},{status:201})}catch(e:any){return Response.json({error:String(e?.message||"Benutzer konnte nicht angelegt werden.").includes("app_users_username_lower_uq")?"Benutzername ist bereits vergeben.":(e?.message||"Benutzer konnte nicht angelegt werden.")},{status:500})}}
