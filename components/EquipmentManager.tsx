"use client";import {FormEvent,useEffect,useState} from "react";type Company={id:string;code:string;name:string};type Eq={id:string;company_id:string;equipment_code:string;name:string;equipment_type:string|null;serial_number:string|null;power_watts:number|null;company_name:string};export default function EquipmentManager(){const[companies,setCompanies]=useState<Company[]>([]),[items,setItems]=useState<Eq[]>([]),[error,setError]=useState(""),[message,setMessage]=useState("");async function load(){try{const[a,b]=await Promise.all([fetch("/api/companies",{cache:"no-store"}),fetch("/api/equipment",{cache:"no-store"})]);setCompanies(await a.json());setItems(await b.json())}catch{setError("Daten konnten nicht geladen werden.")}}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setMessage("");const f=e.currentTarget,res=await fetch("/api/equipment",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(f).entries()))}),j=await res.json();if(!res.ok){setError(j.error||"Fehler");return}f.reset();setMessage("Gerät wurde angelegt.");await load()}const label=(t:string|null)=>t==="technical"?"Technische Trocknung":t==="insulation"?"Dämmschichttrocknung":t==="universal"?"Beide Bereiche":"Sonstige";return <section className="grid cols2"><form className="card" onSubmit={submit}><h2>Gerät anlegen</h2>{error&&<div className="error">{error}</div>}{message&&<div className="notice">{message}</div>}<div className="formGrid" style={{marginTop:12}}><div className="field"><label>Firma *</label><select name="company_id" required defaultValue=""><option value="" disabled>Firma auswählen</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Gerätenummer *</label><input name="equipment_code" required placeholder="z. B. TTK-01"/></div><div className="field full"><label>Bezeichnung *</label><input name="name" required placeholder="z. B. Kondenstrockner Trotec TTK 166 ECO"/></div><div className="field"><label>Kategorie</label><select name="equipment_type" defaultValue="technical"><option value="technical">Technische Trocknung</option><option value="insulation">Dämmschichttrocknung</option><option value="universal">Beide Bereiche</option></select></div><div className="field"><label>Seriennummer</label><input name="serial_number"/></div><div className="field"><label>Leistung Watt</label><input name="power_watts" inputMode="decimal"/></div></div><button className="button success" style={{marginTop:14}}>Gerät speichern</button></form><div className="card"><h2>Vorhandene Geräte</h2>{items.length===0?<div className="empty">Noch keine Geräte angelegt.</div>:items.map(x=><div key={x.id}><strong>{x.equipment_code} · {x.name}</strong><div className="caseMeta">{x.company_name} · {label(x.equipment_type)}{x.serial_number?<><br/>Seriennr.: {x.serial_number}</>:null}{x.power_watts?<><br/>{x.power_watts} W</>:null}</div><div className="divider"/></div>)}</div></section>}
