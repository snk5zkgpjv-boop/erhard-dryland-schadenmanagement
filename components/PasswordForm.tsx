"use client";import {FormEvent,useState} from "react";import {useRouter,useSearchParams} from "next/navigation";
export function ChangePasswordForm(){const[msg,setMsg]=useState(""),router=useRouter();async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f).entries()) as Record<string,string>;if(d.newPassword!==d.confirm)return setMsg("Die neuen Kennwörter stimmen nicht überein.");const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(d)}),b=await r.json();if(!r.ok)return setMsg(b.error);router.replace("/login");router.refresh()}return <form className="card authCard" onSubmit={go}><h1>Mein Konto</h1>{msg&&<div className="error">{msg}</div>}<div className="field"><label>Bisheriges Kennwort</label><input type="password" name="currentPassword" required/></div><div className="field"><label>Neues Kennwort</label><input type="password" name="newPassword" minLength={10} required/></div><div className="field"><label>Neues Kennwort wiederholen</label><input type="password" name="confirm" minLength={10} required/></div><button className="button">Kennwort ändern</button></form>}
export function ResetPasswordForm(){const[msg,setMsg]=useState(""),sp=useSearchParams(),router=useRouter();async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f).entries()) as Record<string,string>;if(d.password!==d.confirm)return setMsg("Die Kennwörter stimmen nicht überein.");const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token:sp.get("token"),password:d.password})}),b=await r.json();if(!r.ok)return setMsg(b.error);router.replace("/login")}return <form className="card authCard" onSubmit={go}><h1>Neues Kennwort</h1>{msg&&<div className="error">{msg}</div>}<div className="field"><label>Neues Kennwort</label><input type="password" name="password" minLength={10} required/></div><div className="field"><label>Wiederholen</label><input type="password" name="confirm" minLength={10} required/></div><button className="button">Kennwort speichern</button></form>}
