"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";
export default function SetupForm(){const[err,setErr]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr("");const f=e.currentTarget,d=Object.fromEntries(new FormData(f).entries()) as any;if(d.password!==d.password2){setErr("Die Passwörter stimmen nicht überein.");setBusy(false);return}const r=await fetch("/api/auth/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(d)});const j=await r.json();if(!r.ok){setErr(j.error||"Einrichtung fehlgeschlagen.");setBusy(false);return}router.replace("/");router.refresh()}return <form className="card authCard" onSubmit={submit}><h1>Ersten Admin einrichten</h1><p className="muted">Dieser Benutzer darf Benutzer verwalten, Schäden löschen und die KI-Funktionen verwenden.</p>{err&&<div className="error">{err}</div>}<div className="field"><label>Anzeigename</label><input name="display_name" required placeholder="z. B. Robert Erhard"/></div><div className="field"><label>Benutzername</label><input name="username" required autoComplete="username"/></div><div className="field"><label>E-Mail (optional)</label><input type="email" name="email"/></div><div className="field"><label>Passwort</label><input type="password" name="password" minLength={8} required autoComplete="new-password"/></div><div className="field"><label>Passwort wiederholen</label><input type="password" name="password2" minLength={8} required autoComplete="new-password"/></div><button className="button success" disabled={busy}>{busy?"Einrichten …":"Admin anlegen"}</button></form>}
