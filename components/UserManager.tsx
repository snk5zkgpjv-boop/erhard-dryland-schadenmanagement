"use client";import {FormEvent,useEffect,useState} from "react";
type U={id:string;username:string;display_name:string;email:string|null;role:"admin"|"techniker"|"buero";active:boolean};
const labels={admin:"Admin",techniker:"Techniker",buero:"Büro"};
export default function UserManager(){const[users,setUsers]=useState<U[]>([]),[err,setErr]=useState(""),[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/users",{cache:"no-store"});const j=await r.json();if(r.ok)setUsers(j);else setErr(j.error||"Fehler")}useEffect(()=>{load()},[]);
 async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();setErr("");setMsg("");const f=e.currentTarget,r=await fetch("/api/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(f).entries()))}),j=await r.json();if(!r.ok){setErr(j.error||"Fehler");return}f.reset();setMsg("Benutzer angelegt.");load()}
 async function patch(id:string,b:any){const r=await fetch(`/api/users/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)});if(!r.ok){const j=await r.json();setErr(j.error||"Änderung fehlgeschlagen.");return}load()}
 return <section className="grid cols2"><form className="card" onSubmit={create}><h2>Benutzer anlegen</h2>{err&&<div className="error">{err}</div>}{msg&&<div className="notice">{msg}</div>}<div className="formGrid"><div className="field"><label>Anzeigename *</label><input name="display_name" required/></div><div className="field"><label>Benutzername *</label><input name="username" required/></div><div className="field"><label>E-Mail</label><input name="email" type="email"/></div><div className="field"><label>Rolle *</label><select name="role" defaultValue="techniker"><option value="techniker">Techniker</option><option value="buero">Büro</option><option value="admin">Admin</option></select></div><div className="field full"><label>Startpasswort *</label><input type="password" name="password" minLength={8} required/></div></div><button className="button success">Benutzer speichern</button></form><div className="card"><h2>Benutzerrollen</h2><p className="muted small"><strong>Admin:</strong> alles inkl. KI, Geräte, Benutzer und Löschen. <strong>Techniker:</strong> Schadenaufnahme, Fotos, Messungen, Trocknung und Rapporte. <strong>Büro:</strong> Angebote, Rechnungen und Artikelliste.</p>{users.map(u=><div key={u.id} className="userRow"><div><strong>{u.display_name}</strong><div className="caseMeta">@{u.username}{u.email?` · ${u.email}`:""}</div></div><select value={u.role} onChange={e=>patch(u.id,{role:e.target.value})}>{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><label className="toggleLabel"><input type="checkbox" checked={u.active} onChange={e=>patch(u.id,{active:e.target.checked})}/> aktiv</label></div>)}</div></section>}
